import { CustomAdBanner } from "@/components/features/ads/CustomAdBanner";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Ads() {
  return (
    <SafeAreaView>
      <CustomAdBanner />
    </SafeAreaView>
  );
}
